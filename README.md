# `kabs`

`kabs` uses the lattice Boltzmann method to solve single phase creeping flow through a volumetric image of a porous material. The solution of this simulation can then be used to find the absolute permeability coefficient, $K_{abs}$, hence the name of this package.


