[project]
name = "kabs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "imageio>=2.37.2",
    "ipykernel>=7.2.0",
    "meshio>=5.3.5",
    "numpy>=2.4.2",
    "pyevtk>=1.6.0",
    "taichi>=1.7.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
